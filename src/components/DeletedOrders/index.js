import * as React from "react";
import Typography from "@mui/material/Typography";

export default function DeletedOrders(props) {
  return (
    <div>
      <Typography paragraph>There are no deleted orders yet.</Typography>
    </div>
  );
}
