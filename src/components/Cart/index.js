import * as React from "react";
import Typography from "@mui/material/Typography";

export default function Cart(props) {
  return (
    <div>
      <Typography paragraph>The Cart is empty.</Typography>
    </div>
  );
}
