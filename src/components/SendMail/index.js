import * as React from "react";
import Typography from "@mui/material/Typography";

export default function SendMail(props) {
  return (
    <div>
      <Typography paragraph>Send Mail content should be here.</Typography>
    </div>
  );
}
