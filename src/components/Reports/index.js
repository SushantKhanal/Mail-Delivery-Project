import * as React from "react";
import Typography from "@mui/material/Typography";

export default function Reports(props) {
  return (
    <div>
      <Typography paragraph>No Past Orders to Show.</Typography>
    </div>
  );
}
